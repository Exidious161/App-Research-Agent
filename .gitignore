# Never commit the API key.
.env

.venv/
__pycache__/
*.pyc

# Research output IS the deliverable — results.csv, patterns.json,
# verification.json, human_review.csv and index.html are tracked on purpose so
# the page can be rebuilt and audited from the repo alone.
run.log
coverage.log
